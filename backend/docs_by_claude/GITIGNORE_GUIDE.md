# 📝 .gitignore 更新說明

## ✅ 已添加的 Docker 相關忽略項目

### 為什麼需要更新 .gitignore？

使用 Docker 後，會產生一些**不應該提交到 Git** 的文件：

1. **日誌文件** - 運行時產生，每次都不同
2. **臨時文件** - 容器內產生的臨時數據
3. **本地配置** - 開發者個人的環境設置
4. **構建緩存** - Docker 構建過程產生的中間文件

## 📋 新增的忽略規則

### Python 相關
```gitignore
__pycache__/      # Python 編譯緩存
*.py[cod]         # Python 編譯文件
*.pyo             # Python 優化文件
*.ipynb_checkpoints/  # Jupyter Notebook 檢查點
```

### IDE 相關
```gitignore
.vscode/          # VS Code 設置
.idea/            # PyCharm/IntelliJ 設置
```

### 環境變量
```gitignore
.env              # 環境變量文件（可能包含敏感信息）
.env.local        # 本地環境變量
```

### OS 相關
```gitignore
.DS_Store         # macOS 系統文件
Thumbs.db         # Windows 縮圖緩存
```

### Docker 相關 🆕
```gitignore
*.log             # 所有日誌文件
/tmp/             # 臨時目錄
/backend/logs/    # 後端日誌目錄
cloudflared.log   # Cloudflare Tunnel 日誌

/backend/data/    # 數據目錄（如果使用持久化）
/backend/temp/    # 臨時文件目錄
```

## ✅ 應該提交的文件

### Docker 配置文件（重要！）
- ✅ `Dockerfile` - 容器映像定義
- ✅ `docker-compose.yml` - 服務編排配置
- ✅ `.dockerignore` - Docker 構建忽略規則
- ✅ `start.sh` - 容器啟動腳本
- ✅ `requirements.txt` - Python 依賴

### 自動化腳本
- ✅ `start-docker.ps1` - Windows 啟動腳本
- ✅ `stop-docker.ps1` - Windows 停止腳本

### 應用程式代碼
- ✅ `testing_backend.py` - Flask 後端
- ✅ `frontend/index.html` - 前端頁面
- ✅ `frontend/script.js` - 前端邏輯
- ✅ `frontend/style.css` - 樣式表

### 文檔
- ✅ `README.md` - 專案說明
- ✅ `backend/docs_by_claude/*.md` - 所有文檔

## ❌ 不應該提交的文件

### 運行時產生的文件
- ❌ `*.log` - 日誌文件
- ❌ `/backend/logs/*` - 日誌目錄內容
- ❌ `cloudflared.log` - Tunnel 日誌
- ❌ `/tmp/*` - 臨時文件

### 本地環境
- ❌ `.env` - 可能包含敏感資訊
- ❌ `.vscode/settings.json` - 個人 IDE 設置

### 構建產物
- ❌ `__pycache__/` - Python 緩存
- ❌ `*.pyc` - 編譯的 Python 文件

## 🔍 檢查當前狀態

### 查看哪些文件會被提交
```bash
git status
```

### 查看被忽略的文件
```bash
git status --ignored
```

### 檢查特定文件是否被忽略
```bash
git check-ignore -v 文件名
```

## 📦 建議的 Git 工作流程

### 1. 檢查狀態
```bash
git status
```

### 2. 添加文件
```bash
# 添加所有更改（會自動排除 .gitignore 中的文件）
git add .

# 或者選擇性添加
git add backend/Dockerfile
git add backend/docker-compose.yml
```

### 3. 提交
```bash
git commit -m "feat: 新增 Docker 部署支持"
```

### 4. 推送
```bash
git push origin main
```

## 🛡️ 安全提醒

### 絕對不要提交的內容：

1. **敏感信息**
   - API 密鑰
   - 資料庫密碼
   - OAuth 客戶端密鑰
   - 私鑰文件

2. **個人數據**
   - 用戶資料
   - 日誌中的個人信息
   - 測試用的真實數據

3. **大型文件**
   - 資料庫文件
   - 大型日誌文件
   - Docker 映像（使用 Docker Hub 或 Registry）

## 📚 進階：環境變量管理

如果需要使用 `.env` 文件，建議：

1. 創建 `.env.example` 作為模板：
```bash
# .env.example
API_BASE_URL=https://your-cloudflare-url.trycloudflare.com
GOOGLE_CLIENT_ID=your-google-client-id
PORT=5001
```

2. 開發者複製並修改：
```bash
cp .env.example .env
# 然後編輯 .env 填入真實值
```

3. 提交 `.env.example`，忽略 `.env`

## ✅ 檢查清單

在提交前確認：

- [ ] `.gitignore` 已更新
- [ ] 沒有 `.log` 文件被追蹤
- [ ] 沒有 `__pycache__` 目錄
- [ ] 沒有 `.env` 文件（如果包含敏感信息）
- [ ] Docker 配置文件已包含
- [ ] 文檔已添加
- [ ] 測試腳本已包含

## 🔄 如果不小心提交了不該提交的文件

### 從 Git 歷史中移除
```bash
# 停止追蹤但保留本地文件
git rm --cached 文件名

# 提交更改
git commit -m "chore: 移除不該追蹤的文件"
```

### 清理歷史（謹慎使用！）
```bash
# 如果包含敏感信息，需要從歷史中完全刪除
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch 文件名" \
  --prune-empty --tag-name-filter cat -- --all
```

---

**更新日期：** 2025-11-12
**適用版本：** Docker 部署版本
