# 🏗️ Docker 架構圖

## 整體架構

```
┌─────────────────────────────────────────────────────────────────┐
│                        開發者電腦                                │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    專案目錄                               │ │
│  │                                                          │ │
│  │  📁 frontend/                                            │ │
│  │     ├── index.html                                       │ │
│  │     ├── script.js  ◄── 需要更新 API_BASE_URL            │ │
│  │     └── style.css                                        │ │
│  │                                                          │ │
│  │  📁 backend/                                             │ │
│  │     ├── Dockerfile        ◄── Docker 配置                │ │
│  │     ├── docker-compose.yml                               │ │
│  │     ├── start.sh          ◄── 啟動腳本                   │ │
│  │     ├── testing_backend.py                               │ │
│  │     └── requirements.txt                                 │ │
│  │                                                          │ │
│  │  📄 start-docker.ps1    ◄── 一鍵啟動！                   │ │
│  │  📄 stop-docker.ps1                                      │ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                           │                                    │
│                           │ 執行 start-docker.ps1              │
│                           ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │               Docker Desktop                              │ │
│  │                                                          │ │
│  │    ┌─────────────────────────────────────────────┐      │ │
│  │    │  Docker Container                            │      │ │
│  │    │  (heart-monitor-backend)                     │      │ │
│  │    │                                              │      │ │
│  │    │  ┌────────────────────────────────┐         │      │ │
│  │    │  │  Flask Backend                 │         │      │ │
│  │    │  │  testing_backend.py            │         │      │ │
│  │    │  │  Port: 5001                    │         │      │ │
│  │    │  │  ├── REST API                  │         │      │ │
│  │    │  │  └── WebSocket                 │         │      │ │
│  │    │  └────────────┬───────────────────┘         │      │ │
│  │    │               │                              │      │ │
│  │    │  ┌────────────▼───────────────────┐         │      │ │
│  │    │  │  Cloudflare Tunnel             │         │      │ │
│  │    │  │  cloudflared                   │         │      │ │
│  │    │  │  ├── 監聽 localhost:5001        │         │      │ │
│  │    │  │  └── 建立安全隧道              │         │      │ │
│  │    │  └────────────┬───────────────────┘         │      │ │
│  │    │               │                              │      │ │
│  │    └───────────────┼──────────────────────────────┘      │ │
│  │                    │                                      │ │
│  └────────────────────┼──────────────────────────────────────┘ │
│                       │                                        │
└───────────────────────┼────────────────────────────────────────┘
                        │
                        │ HTTPS over Internet
                        ▼
        ┌───────────────────────────────┐
        │   Cloudflare Edge Network     │
        │   🌐 全球 CDN                 │
        └───────────────┬───────────────┘
                        │
                        │ HTTPS Public URL
                        │ https://xxxxx.trycloudflare.com
                        ▼
        ┌───────────────────────────────┐
        │   使用者瀏覽器                │
        │   訪問 frontend/index.html    │
        │   ├── 載入 HTML/CSS/JS        │
        │   └── API 呼叫到 Tunnel URL   │
        └───────────────────────────────┘
```

## 啟動流程詳解

```
使用者執行: .\start-docker.ps1
         │
         ▼
┌─────────────────────────────────────────────────────┐
│  階段 1: 環境檢查                                    │
│  ├── 檢查 Docker 是否安裝                           │
│  ├── 檢查 Docker 服務是否運行                       │
│  └── 驗證目錄結構                                   │
└────────────────┬────────────────────────────────────┘
                 │ ✓ 通過
                 ▼
┌─────────────────────────────────────────────────────┐
│  階段 2: Docker 建置                                │
│  ├── 讀取 Dockerfile                                │
│  ├── 下載 Python 3.11 基礎映像                      │
│  ├── 安裝系統套件 (curl, wget)                      │
│  ├── 下載並安裝 cloudflared                         │
│  ├── 安裝 Python 依賴 (requirements.txt)            │
│  └── 複製應用程式文件                               │
└────────────────┬────────────────────────────────────┘
                 │ ✓ 映像建立完成
                 ▼
┌─────────────────────────────────────────────────────┐
│  階段 3: 容器啟動                                    │
│  ├── docker-compose up --build -d                   │
│  ├── 創建網路 (heart-monitor-network)               │
│  └── 啟動容器 (heart-monitor-backend)               │
└────────────────┬────────────────────────────────────┘
                 │ ✓ 容器運行中
                 ▼
┌─────────────────────────────────────────────────────┐
│  階段 4: 服務啟動 (容器內)                          │
│  ├── 執行 start.sh                                  │
│  ├── 啟動 Flask Backend (背景執行)                  │
│  │   └── 監聽 0.0.0.0:5001                         │
│  └── 啟動 Cloudflare Tunnel (背景執行)              │
│      └── 連接到 localhost:5001                      │
└────────────────┬────────────────────────────────────┘
                 │ ✓ 服務啟動完成
                 ▼
┌─────────────────────────────────────────────────────┐
│  階段 5: URL 提取                                    │
│  ├── 等待 Tunnel 建立連接                           │
│  ├── 從日誌中提取 Tunnel URL                        │
│  └── 複製 URL 到剪貼簿                              │
└────────────────┬────────────────────────────────────┘
                 │ ✓ URL 已獲取
                 ▼
┌─────────────────────────────────────────────────────┐
│  階段 6: 顯示結果                                    │
│  ├── 顯示 Cloudflare Tunnel URL (HTTPS)            │
│  ├── 顯示本地 URL (HTTP)                            │
│  ├── 提供下一步指引                                 │
│  └── 選項: 查看即時日誌                             │
└─────────────────────────────────────────────────────┘
         │
         ▼
    🎉 完成！
```

## 網路連接流程

```
┌─────────────────┐
│  瀏覽器         │
│  (Frontend)     │
└────────┬────────┘
         │
         │ HTTPS Request
         │ GET /api/v1/health/summary
         │ Authorization: Bearer TOKEN
         ▼
┌─────────────────────────────────────┐
│  Cloudflare Edge                    │
│  (全球 CDN)                         │
│  ├── SSL/TLS 終止                   │
│  ├── DDoS 防護                      │
│  └── 快取 (靜態資源)                │
└────────┬────────────────────────────┘
         │
         │ 加密隧道 (Tunnel)
         ▼
┌─────────────────────────────────────┐
│  Docker Container                   │
│                                     │
│  ┌────────────────────────────┐    │
│  │  cloudflared               │    │
│  │  接收請求                  │    │
│  └────────┬───────────────────┘    │
│           │                         │
│           │ HTTP (localhost)        │
│           ▼                         │
│  ┌────────────────────────────┐    │
│  │  Flask Backend             │    │
│  │  處理請求                  │    │
│  │  ├── 驗證 Token            │    │
│  │  ├── 查詢資料              │    │
│  │  └── 返回 JSON             │    │
│  └────────┬───────────────────┘    │
│           │                         │
└───────────┼─────────────────────────┘
            │
            │ HTTP Response (JSON)
            ▼
  (原路返回到瀏覽器)
```

## 資料流動

```
┌──────────────────────────────────────────────────────┐
│  請求流程                                             │
│                                                      │
│  Browser → Cloudflare Edge → Tunnel → Flask → Response
│                                                      │
│  ┌─────────┐   ┌──────────┐   ┌───────┐   ┌──────┐│
│  │瀏覽器   │─→ │Cloudflare│─→ │Tunnel │─→ │Flask ││
│  │         │   │Edge      │   │       │   │      ││
│  └─────────┘   └──────────┘   └───────┘   └──────┘│
│       ▲                                        │    │
│       │        ← ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘    │
│       │                                             │
│       └──── JSON Response (經過同樣路徑返回)        │
│                                                      │
└──────────────────────────────────────────────────────┘
```

## Docker 容器內部結構

```
┌─────────────────────────────────────────────────────┐
│  Container: heart-monitor-backend                   │
│                                                     │
│  📁 /app (工作目錄)                                 │
│  ├── testing_backend.py    ◄── Flask 應用         │
│  ├── requirements.txt                              │
│  └── start.sh              ◄── 啟動腳本            │
│                                                     │
│  📁 /usr/local/bin                                  │
│  ├── python3               ◄── Python 3.11        │
│  ├── pip                                           │
│  └── cloudflared           ◄── Tunnel 客戶端       │
│                                                     │
│  📁 /tmp                                            │
│  └── cloudflared.log       ◄── Tunnel 日誌        │
│                                                     │
│  🔌 開放端口                                        │
│  └── 5001:5001             ◄── Flask Backend      │
│                                                     │
│  🏃 運行中的程序                                    │
│  ├── PID 1: start.sh                               │
│  ├── PID 2: python testing_backend.py             │
│  └── PID 3: cloudflared tunnel                     │
│                                                     │
└─────────────────────────────────────────────────────┘
```

## 文件依賴關係

```
start-docker.ps1
    │
    ├─→ 檢查 Docker
    │
    └─→ docker-compose.yml
            │
            └─→ Dockerfile
                    │
                    ├─→ requirements.txt
                    │       └─→ flask
                    │       └─→ flask-cors
                    │       └─→ flask-sock
                    │       └─→ eventlet
                    │
                    ├─→ testing_backend.py
                    │
                    └─→ start.sh
                            │
                            ├─→ 啟動 Flask
                            │
                            └─→ 啟動 Cloudflared
                                    │
                                    └─→ 生成 Tunnel URL
```

## 優勢示意圖

```
傳統方式:
┌────────┐   ┌────────┐   ┌──────────┐   ┌────────┐
│安裝    │ → │配置    │ → │啟動      │ → │開放    │
│Python  │   │環境    │   │服務      │   │端口    │
└────────┘   └────────┘   └──────────┘   └────────┘
   ⏱️10分      ⏱️5分         ⏱️2分          ⚠️安全風險

Docker 方式:
┌──────────────────────────────────────────────────┐
│  .\start-docker.ps1                              │
│                                                  │
│  自動完成所有步驟 + HTTPS + 安全隔離            │
└──────────────────────────────────────────────────┘
                     ⏱️2分鐘
```

## 安全層級

```
┌─────────────────────────────────────────┐
│  外部使用者                              │
└────────────┬────────────────────────────┘
             │
             │ ① HTTPS 加密連接
             ▼
┌─────────────────────────────────────────┐
│  Cloudflare Edge (DDoS 防護)            │
└────────────┬────────────────────────────┘
             │
             │ ② 加密隧道
             ▼
┌─────────────────────────────────────────┐
│  Cloudflared (Tunnel)                   │
└────────────┬────────────────────────────┘
             │
             │ ③ 本地連接 (僅容器內)
             ▼
┌─────────────────────────────────────────┐
│  Flask Backend (容器隔離)               │
└─────────────────────────────────────────┘

✅ 多層安全保護
✅ 無需開放防火牆端口
✅ 容器環境隔離
✅ 自動 HTTPS
```

---

這個架構提供了：
- 🚀 **快速部署** - 一個命令搞定
- 🔒 **安全性** - 多層保護
- 🌐 **全球訪問** - Cloudflare CDN
- 🐳 **易於管理** - Docker 容器化
- 📊 **可監控** - 即時日誌
